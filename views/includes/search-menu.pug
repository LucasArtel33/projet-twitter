div(class="search-menu d-flex flex-column")
  if users && users.length
    each user in users
      a(class="search-menu-item d-flex flex-row align-items-center justify-content-around pointer-cursor" href='/users/' + user.username)
        img(class="img-profile-search" src=user.avatar)
        span= user.username
  else
    span Aucun utilisateur trouvé